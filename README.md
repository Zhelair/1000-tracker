# 1000-tracker

Счётчик для карточной игры «Тысяча» (1000).  
**Не игра в карты.** Вы играете реальными картами — приложение только фиксирует правила и ведёт счёт на 3 устройствах.

## Быстрый старт (локально)
1) Заполните `supabase.js` (URL + anon key)
2) Откройте `index.html` (через любой простой сервер, например VS Code Live Server)

## Deploy на GitHub Pages
1) Push в репозиторий
2) Settings → Pages → Deploy from branch → `main` → `/root`
3) Откройте ссылку вида: `https://USERNAME.github.io/1000-tracker`

## Supabase
Таблицы: `rooms`, `players`, `rules`, `rounds`, `totals`  
Realtime включен для этих таблиц.

### Важно про ключи
- `anon public key` можно хранить во фронтенде.
- `service_role key` — НИКОГДА.

## Логика MVP
- Заказчик: `+заказ` если сыграл, иначе `−заказ`
- Не-заказчики: введённые очки (с округлением, если включено)
- Болты (опционально): 3 болта → штраф `boltsPenalty` и болты сбрасываются
- Самосвал 555 (опционально): если ровно 555 → 0
- Undo: удаляет последний кон и пересчитывает всё с нуля (надёжно)

---

Made for family peace 🙂
